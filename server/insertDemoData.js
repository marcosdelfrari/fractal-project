/**
 * Atalho na raiz da pasta `server/`.
 * O script real está em `server/utills/insertDemoData.js`.
 *
 * Na raiz do repositório:
 *   node server/insertDemoData.js
 *
 * Ou, dentro de `server/`:
 *   npm run db:seed
 */
require("./utills/insertDemoData.js");
