{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && npx prisma generate && npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npm run prisma:migrate:deploy && node server/scripts/conditional-seed.js && node server/app.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}



